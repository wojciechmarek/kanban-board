import { GithubSingInDirective } from './github-sing-in.directive';

describe('GithubSingInDirective', () => {
  it('should create an instance', () => {
    const directive = new GithubSingInDirective();
    expect(directive).toBeTruthy();
  });
});
